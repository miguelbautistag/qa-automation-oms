[pytest]
# Tell pytest where to look for tests
testpaths = tests/ui tests/api
# Add src to the python path so 'from pages.x import y' works
pythonpath = src
addopts = -v --html=reports/report.html --self-contained-html
